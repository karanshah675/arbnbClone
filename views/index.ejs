<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>listings</title>
</head>

<body>
      <form action="/listing/new" method="get">
        <button>Add new</button>
    </form>
  <h3>all listing</h3>
  <% for(d of data){ %>
  <li><a href="listing/<%= d.id %>/show"><%= d.title %></a></li>
  <%}%>
</body>
</html>